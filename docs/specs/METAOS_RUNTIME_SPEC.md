# METAOS Runtime Specification

METAOS runtime executes the exploration loop.

Core runtime components:

loop daemon  
strategy engine  
artifact generator  
metrics evaluator  
mutation engine  

Runtime must support replayable state reconstruction.
